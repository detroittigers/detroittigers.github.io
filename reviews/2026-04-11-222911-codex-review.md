# Codex Review - 2026-04-11

**Mode:** Standard
**Commits reviewed:** 5 (from HEAD~5)
**Rounds:** 1
**Result:** Passed (0 Must-fix)

**Verdict:** needs-attention
**Summary:** The Epic Universe changes are directionally consistent with the new drawer-centric design, and I did not find server-side/data-loss class risks in this static implementation. The main concerns are a new DOM injection trust-boundary issue in the drawer rendering path and a regression where the "best days" items lost native link/button semantics.

## Findings

### Should-fix (1)
- **Drawer innerHTML interpolation** (confidence: 0.91): Drawer cards use innerHTML with data from actuals.json. Low risk since data is from own pipeline, but should use textContent/DOM nodes for defense in depth.

### Nice-to-have (1)
- **Best-days keyboard accessibility** (confidence: 0.98): Best-days rows changed from anchors to divs with click handlers, losing keyboard navigation. Should use button elements.

## Final Feedback

{"verdict":"needs-attention","summary":"The Epic Universe changes are directionally consistent with the new drawer-centric design, and I did not find server-side/data-loss class risks in this static implementation. The main concerns are a new DOM injection trust-boundary issue in the drawer rendering path and a regression where the \"best days\" items lost native link/button semantics.","findings":[{"severity":"medium","title":"Drawer content interpolates data into `innerHTML` without escaping","body":"Several drawer cards are now built with `innerHTML` using values from `record` such as `parkHours`, `weatherEmoji`, `eventName`, and `eventTag`. In this site, those fields come from static data rather than hardcoded literals, so this creates a client-side trust boundary: if the JSON/content pipeline ever emits HTML markup, it will be executed in-page. The previous implementation had less exposure here; this change expands the number of unsanitized insertions.","file":"parks/epic-universe/crowd-calendar/index.html","line_start":1763,"line_end":1789,"confidence":0.91,"recommendation":"Build these cards with DOM nodes and `textContent` instead of `innerHTML`, or escape all interpolated data fields before insertion."},{"severity":"low","title":"Best-days entries are no longer keyboard-accessible or linkable","body":"The best-days rows were changed from anchors to plain `div` elements with only a click handler. That removes native keyboard activation, focus behavior, open-in-new-tab support, and shareable link semantics. It is a behavioral regression from the previous implementation and will make this list unusable for keyboard-only users.","file":"parks/epic-universe/crowd-calendar/index.html","line_start":1997,"line_end":2018,"confidence":0.98,"recommendation":"Render each row as an `<a>` to the destination page or as a `<button>` with proper keyboard/focus handling. If opening the drawer is the desired behavior, prefer a `<button>` and preserve a separate explicit link for navigation."}],"next_steps":["Replace drawer `innerHTML` construction with safe DOM assembly using `textContent`.","Restore semantic interactive elements for the best-days list and verify keyboard navigation on all propagated park pages.","Spot-check the other 14 park pages for the same two issues before merging."]}
