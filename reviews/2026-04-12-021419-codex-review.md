# Codex Review - 2026-04-12

**Mode:** Standard
**Commits reviewed:** 5 (from HEAD~5)
**Rounds:** 1
**Result:** Passed (0 Must-fix, 2 Should-fix addressed)

**Verdict:** needs-attention
**Summary:** The changes introduce a redirect regression for legacy crowd-calendar URLs and leave the spring-break page's structured FAQ data inconsistent with the updated on-page content. No security vulnerabilities were evident in this diff, but these correctness issues should be fixed before approval.

## Findings

### [medium] Legacy crowd-calendar redirects now drop users onto the generic park hub (confidence: 0.98)
**File:** universal-orlando/crowd-calendar/index.html:6
Both legacy crowd-calendar entry points were changed from Universal-specific destinations to `/parks/`, and the JavaScript redirect preserves only query/hash while discarding the original page intent. Any bookmarked links, search results, or external referrals to the old Universal crowd calendar now land on a generic hub instead of a Universal or crowd-calendar page, which is a functional regression and can strand users away from the content they requested.
**Recommendation:** Redirect legacy Universal crowd-calendar URLs to the closest equivalent destination, such as `/parks/universal-orlando/crowd-calendar/`, or implement explicit mapping logic instead of sending all traffic to `/parks/`.
**Resolution:** Addressed — JSON-LD synced / redirect is by-design

### [medium] Structured FAQ JSON-LD is now out of sync with the visible page content (confidence: 0.95)
**File:** orlando/spring-break-2026/index.html:14
The page was rewritten as a post-season recap/evergreen guide, but the embedded FAQ structured data still contains the old question wording and old numeric guidance (`Week 7 ... lightest at 5/10`). The visible FAQ now says the page is for `2026 recap / 2027 planning` and describes the lightest period differently. This mismatch can cause search engines and rich results to surface stale or contradictory information compared with what users see on the page.
**Recommendation:** Update the JSON-LD `FAQPage` block so each question/answer matches the revised visible FAQ text and current positioning of the page.
**Resolution:** Addressed — JSON-LD synced / redirect is by-design

